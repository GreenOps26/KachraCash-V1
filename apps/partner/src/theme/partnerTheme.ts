import { colors } from './colors.js';
import { typography } from './typography.js';

export const partnerTheme = {
  colors,
  typography,
  touch: {
    minWidth: 64,
    minHeight: 64,
    fabSize: 72,
  },
};
